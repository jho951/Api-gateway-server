name: gateway-service

services:
  gateway-service:
    image: ${GATEWAY_IMAGE:-gateway-service:dev}
    networks:
      service-shared:
        aliases:
          - gateway-service
          - gateway

networks:
  service-shared:
    external: true
    name: ${SERVICE_SHARED_NETWORK:-${BACKEND_SHARED_NETWORK:-${MSA_SHARED_NETWORK:-service-backbone-shared}}}
